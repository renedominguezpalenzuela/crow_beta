<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}{% endblock %} - Crow</title>
    <link rel="stylesheet" href="{{ asset('assets/css/material-dashboard.min.css?v=2.1.0') }}">

   
   {# <link rel="stylesheet"
          href="{{ asset('https://fonts.googleapis.com/css?family=Roboto:300,400,500,700|Roboto+Slab:400,700|Material+Icons') }}">
    <link rel="stylesheet"
          href="{{ asset('https://maxcdn.bootstrapcdn.com/font-awesome/latest/css/font-awesome.min.css') }}">

#}     

   {# Mi fichero de estilos #}
    <link rel="stylesheet" href="{{ asset('assets/css/myestilos.css') }}">
    {% block css %}{% endblock %}

  
</head>
<body class="dark-edition">
<div class="wrapper container-fluid">
    <div class="sidebar" data-color="purple" data-background-color="black" data-image="{{ asset('images/interface/warrior.jpg') }}">
        <!--
          Tip 1: You can change the color of the sidebar using: data-color="purple | azure | green | orange | danger"

          Tip 2: you can also add an image using data-image tag
      -->
        <div class="logo">
            <a href="#" class="simple-text logo-normal">
                Crow
            </a>
        </div>
        <div class="sidebar-wrapper">
            <ul class="nav">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('dashboard_player_army') }}">
                            <p>Player's Army</p>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('dashboard_kingdom_court') }}">
                            <p>Kingdom Court</p>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('dashboard_world_map') }}">
                            <p>World Map</p>
                        </a>
                    </li>

                {% endif %}
                {#<li class="nav-item active">#}
                    {#<a class="nav-link" href="#">#}
                        {#<i class="material-icons">dashboard</i>#}
                        {#<p>Dashboard</p>#}
                    {#</a>#}
                {#</li>#}
                {#<li class="nav-item ">#}
                    {#<a class="nav-link" href="#">#}
                        {#<i class="material-icons">person</i>#}
                        {#<p>User Profile</p>#}
                    {#</a>#}
                {#</li>#}
            </ul>
        </div>
    </div>
    <div class="main-panel">
        <!-- Navbar -->
        <nav class="navbar navbar-expand-lg navbar-transparent navbar-absolute fixed-top " id="navigation-example">
            <div class="container-fluid">
                <div class="navbar-wrapper">
                   {# <a class="navbar-brand" href="{{ path('dashboard') }}">Dashboard</a>#}
                </div>
                <button class="navbar-toggler" type="button" data-toggle="collapse" aria-controls="navigation-index"
                        aria-expanded="false" aria-label="Toggle navigation" data-target="#navigation-example">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="navbar-toggler-icon icon-bar"></span>
                    <span class="navbar-toggler-icon icon-bar"></span>
                    <span class="navbar-toggler-icon icon-bar"></span>
                </button>
                <div class="collapse navbar-collapse justify-content-end">
                    {#
                    <form class="navbar-form">
                        <div class="input-group no-border">
                            <input type="text" value="" class="form-control" placeholder="Search...">
                            <button type="submit" class="btn btn-default btn-round btn-just-icon">
                                <i class="material-icons">search</i>
                                <div class="ripple-container"></div>
                            </button>
                        </div>
                    </form>#}
                   
                    <ul class="navbar-nav">
                     {#
                        <li class="nav-item">
                            <a class="nav-link" href="javascript:void(0)">
                                <i class="material-icons">dashboard</i>
                                <p class="d-lg-none d-md-block">
                                    Stats
                                </p>
                            </a>
                        </li>#}
                        {% if is_granted('IS_AUTHENTICATED_FULLY') %}

                        {#
                        <li class="nav-item dropdown">
                            <a class="nav-link" href="javscript:void(0)" id="navbarDropdownMenuLink"
                               data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <i class="material-icons">notifications</i>
                                <span class="notification">5</span>
                                <p class="d-lg-none d-md-block">
                                    Some Actions
                                </p>
                            </a>
                            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                                <a class="dropdown-item" href="javascript:void(0)">Mike John responded to your email</a>
                                <a class="dropdown-item" href="javascript:void(0)">You have 5 new tasks</a>
                                <a class="dropdown-item" href="javascript:void(0)">You're now friend with Andrew</a>
                                <a class="dropdown-item" href="javascript:void(0)">Another Notification</a>
                                <a class="dropdown-item" href="javascript:void(0)">Another One</a>
                            </div>
                        </li>
                        #}
                        <li class="nav-item dropdown">
                            <a class="nav-link" href="javscript:void(0)" id="navbarDropdownMenuLink"
                               data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                <i class="material-icons">account</i>
                                <p class="d-lg-none d-md-block">
                                    Account
                                </p>
                            </a>
                            <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdownMenuLink">
                                <a class="dropdown-item" href="{{ path('user_edit', {'id':app.user.id}) }}">Profile</a>
                                <a class="dropdown-item" href="{{ path('logout') }}">Logout</a>
                            </div>
                        </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>
        <!-- End Navbar -->
        <div class="content">
   {#Contenedor principal#}

   
            <div>
                {% block body %}{% endblock %}
            </div>

        </div>
        <footer class="footer">
            <div class="container-fluid">
                <nav class="float-left">
                    <ul>
                        <li id="date"></li>
                    </ul>
                </nav>
            </div>
        </footer>
        <script>
          {#  const x = new Date().getFullYear();
            let date = document.getElementById('date');
            date.innerHTML = '&copy; ' + x + date.innerHTML;#}
        </script> 
    </div>
</div>
<script src="{{ asset('assets/js/core/jquery.min.js') }}" type="text/javascript"></script>
<script src="{{ asset('assets/js/core/popper.min.js') }}" type="text/javascript"></script>
<script src="{{ asset('assets/js/core/bootstrap-material-design.min.js') }}" type="text/javascript"></script>
<script src="{{ asset('assets/js/plugins/perfect-scrollbar.jquery.min.js') }}" type="text/javascript"></script>
<script src="{{ asset('assets/js/plugins/bootstrap-notify.js') }}" type="text/javascript"></script>
<script src="{{ asset('assets/js/material-dashboard.js') }}" type="text/javascript"></script>
<script>
    {#
     Para los colores de las notificaciones desde el controlador es
     *** 'info', 'danger', 'success', 'warning', 'primary'; ***
     Por lo que es directamente a la clase de bootstrap y no personalizado
     #}
    $(document).ready(function(){
        {% for tipo, mensajes in app.session.flashbag.all() %}
            {% for mensaje in mensajes %}
            $.notify({
                message: '{{ mensaje }}'
            }, {
                type: '{{ tipo }}',
                timer: 3000,
                placement: {
                    from: 'top',
                    align: 'right'
                }
            });

            {% endfor %}
        {% endfor %}

        var url = window.location;
        var element = $('li a').filter(function () {
            return this.href == url || url.href.indexOf(this.href) == 0;
        }).addClass('active').parent();
        if (element.is('li')) {
            element.parent().parent().addClass('active');
            element.parent().addClass('in');
            element.addClass('active');
            element.children().removeClass('active');
        }
    });
</script>
<script>
    $('.main-panel > .content').css('background-image', 'url("{{ asset('images/interface/project_crow_login.jpg') }}")');
    $('.main-panel > .content').css('background-size', 'cover');
    $('.main-panel > .content').css('background-repeat', 'no-repeat');
    $('.main-panel > .content').css('min-height', 'calc(200vh - 123px');

   
    
   // $('.main-panel > .content').css('-webkit-filter', 'grayscale(60%)');
   //  $('.main-panel > .content').css('filter', 'grayscale(60%)');
     //$('.main-panel > .content').css('-webkit-filter', 'brightness(50%)');
     //$('.main-panel > .content').css('filter', 'brightness(50%)');
   //  $('.main-panel > .content').css('-webkit-filter', 'blur(4px)');
   //  $('.main-panel > .content').css('filter', 'blur(4px)');
</script>
{% block js %}{% endblock %}
</body>
</html>
